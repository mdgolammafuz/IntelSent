# -----------------------------
# Python
# -----------------------------
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.log
*.tmp

# Environments
.venv/
.env
.env.*
*.local

# Tests / coverage
.pytest_cache/
.coverage

# -----------------------------
# OS / Editors
# -----------------------------
.DS_Store
.vscode/
.idea/

# -----------------------------
# Prefect (local server/db)
# -----------------------------
.prefect/
.prefect.orion.db
.prefect.orion.db-*

# -----------------------------
# Docker / local config
# -----------------------------
# Keep committed configs (e.g., app.docker.yml). Ignore local override if you create one.
config/app.yaml

# -----------------------------
# Project artifacts
# -----------------------------
artifacts/
.qa/
# (top-level safety; actual data lives under /data)
sec-edgar-filings/

# Reports: ignore all, keep the demo eval
/reports/*
!/reports/dev.eval.json
!/reports/.gitkeep

# -----------------------------
# Application data (keep code, ignore generated)
# -----------------------------
/data/.ingested/
/data/sec-edgar-filings/
/data/sec_catalog/
/data/chunks/

# Keep the Python tools and placeholders inside /data
!/data/*.py
!/data/.gitkeep

# QA: track folder but ignore contents by default, keep the dev set
/data/qa/*
!/data/qa/dev.jsonl

# -----------------------------
# UI
# -----------------------------
/ui/node_modules
/ui/dist
